#include <cstdio>
constexpr int N = 50004;
int c[N], f[N];
int main() {
    int n, q;
    scanf("%d%d", &n, &q);
    for (int i = 1; i <= n; i++) scanf("%d", &c[i]);
    for (int i = 1; i <= n; i++) scanf("%d", &f[i]);
    while (q--) {
        int A, B, l, r;
        scanf("%d%d%d%d", &A, &B, &l, &r);
        if (n == 2) {
            
        }
    }
    return 0;
}