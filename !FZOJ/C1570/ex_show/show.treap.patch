@@ -63,7 +63,10 @@ struct Treap {
     }
     std::pair<int,int> lower_bound(int u, int64_t w) { // Find the first position to insert w, and split into 2 parts
         if (tr[u].lmx <= w) return {0, u};
         if (tr[u].rmn > w) return {u, 0};
         pushdown(u);
-        if (w > tr[u].val) {
-            auto [ll, lr] = std::move(lower_bound(tr[u].ls, w));
-            tr[u].ls = lr;
-            pushup(u);
-            return {ll, u};
-        }
-        auto [rl, rr] = std::move(lower_bound(tr[u].rs, w));
-        tr[u].rs = rl;
-        pushup(u);
-        return {u, rr};
+        if (w > tr[u].val) {
+            std::pair<int,int> __tmp = lower_bound(tr[u].ls, w);
+            int ll = __tmp.first;
+            int lr = __tmp.second;
+            tr[u].ls = lr;
+            pushup(u);
+            return std::pair<int,int>(ll, u);
+        }
+        std::pair<int,int> __tmp = lower_bound(tr[u].rs, w);
+        int rl = __tmp.first;
+        int rr = __tmp.second;
+        tr[u].rs = rl;
+        pushup(u);
+        return std::pair<int,int>(u, rr);
     }
     std::pair<int,int> split(int u, int w) { // Split the tree to {lsize=w, r}
         if (w == 0) return {0, u};
         if (tr[u].cnt == w) return {u, 0};
         // if (tr[u].cnt < w) assert(0);
@@ -71,14 +74,22 @@ struct Treap {
         pushdown(u);
         if (tr[tr[u].ls].cnt >= w) {
-            auto [ll, lr] = std::move(split(tr[u].ls, w));
-            tr[u].ls = lr;
-            pushup(u);
-            return {ll, u};
+            std::pair<int,int> __tmp = split(tr[u].ls, w);
+            int ll = __tmp.first;
+            int lr = __tmp.second;
+            tr[u].ls = lr;
+            pushup(u);
+            return std::pair<int,int>(ll, u);
         }
-        auto [rl, rr] = std::move(split(tr[u].rs, w - tr[tr[u].ls].cnt - 1));
-        tr[u].rs = rl;
-        pushup(u);
-        return {u, rr};
+        std::pair<int,int> __tmp = split(tr[u].rs, w - tr[tr[u].ls].cnt - 1);
+        int rl = __tmp.first;
+        int rr = __tmp.second;
+        tr[u].rs = rl;
+        pushup(u);
+        return std::pair<int,int>(u, rr);
     }
@@ -108,7 +108,10 @@ int main(int argc, char** argv) {
         for (; cur <= k*2 && all[cur].first.first == i; cur++) {
-            auto [ul, ur] = std::move(f.split(rts[rts[0]], all[cur].first.second - lastPos));
+            std::pair<int,int> __tmp1 = f.split(rts[rts[0]], all[cur].first.second - lastPos);
+            int ul = __tmp1.first;
+            int ur = __tmp1.second;
+
             lastPos = all[cur].first.second;
             rts[rts[0]] = ul;
             rts[++rts[0]] = ur;
@@ -122,7 +125,10 @@ int main(int argc, char** argv) {
             int u2 = rts[j+1];
             if (!u1) continue;
             int64_t last = f.inquireLast(u1);
-            auto [ul, ur] = std::move(f.lower_bound(u2, last));
+            std::pair<int,int> __tmp2 = f.lower_bound(u2, last);
+            int ul = __tmp2.first;
+            int ur = __tmp2.second;
+
             f.tr[ul].pull(0, last);
             rts[j+1] = f.merge(u1, f.merge(ul, ur));
