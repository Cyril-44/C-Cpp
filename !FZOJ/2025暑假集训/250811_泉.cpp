#include <cstdio>
#include <cstring>
#include <algorithm>
constexpr int N = 100005;
int a[N];
int main() {
    int n, k;
    scanf("%d%d", &n, &k);
    for (int i = 1; i <= n; i++) scanf("%d", a+i);
    
    return 0;
}