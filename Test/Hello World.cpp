#include <bits/stdc++.h>
int main() {
    std::vector<std::string> vec = {"Hello", "World"};
    for (auto i : vec)
        std::cout << i << ' ';
    std::cout << '!' << std::endl;
    
    return 0;
}