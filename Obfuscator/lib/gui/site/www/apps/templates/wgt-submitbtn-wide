<%
    use obfui::cmn;
    my ($title,$name,$hint) = @_;
    $title ||= __('OK');
%>
<input type=submit class=button-wide <%$ $name ? "name=$name" : '' %>
        <% if ($hint) { %>
                onmouseover="return overlib('<%= $hint %>')" onmouseout="return nd();"
        <% }; %>
	value="<%= $title %>" >
