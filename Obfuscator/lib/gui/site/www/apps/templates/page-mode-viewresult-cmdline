<% 
    my ($prj,$nm,$cmdline) = @_;
    use obfui::cmn;

    my $m = $prj->{modes}->{$nm};
    my $title = $m->{title};
%>
<h2><%= sprintf(__('Viewing resultant commandline for \'%s\':'), $title) %></h2>

<div class=consoleoutput>
<%= $cmdline %>
</div>

<p>

<% $forge->include('wgt-backbtn',__('Back')) %>