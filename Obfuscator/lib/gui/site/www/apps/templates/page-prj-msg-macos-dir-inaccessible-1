<% 
    {my $mod = $ENV{REQUEST_URI} =~ m,^/ws-ui, ? "ui" : "obfui" ;eval("use " . $mod . "::cmn;");}; #import proper module
%>

<% $forge->include('wgt-load-semanticui') %>

<h3 class="ui center aligned icon header" >
  <i class="remove circle icon negative"></i>
    <%= __('Please grant access to special folders on macOS') %>
</h3>

<div class="main ui container">

<%$ __('Your macOS currently does not grant our product access to special folders like <tt>Desktop</tt> or <tt>Documents</tt>, but your project has files in those directories.') %>
<br/>
<%$ __('Please move your project to non-special folder, e.g. <tt>~/MyProjects</tt> folder, or grant access to our app to those special folders in System Preferences.') %>


    <div class="ui accordion container">
        <div class="title"><i class="dropdown icon"></i><%= __('How to grant access to special folders to our app') %></div>
        <div class="content">
	    <ol>
	    <li><%$ __('Open  Apple menu <i class="apple icon"></i> &rArr; System Preferences, click Security &amp; Privacy, then click Privacy. Select "Full Disk Access".') %></li>
	    <li><%= __('Unlock settings by pressing the lock button in the left downside corner.') %></li>
	    <li><%$ __('Open Terminal and type <tt>open -R /usr/bin/perl</tt><br/>A new Finder window should open with file named "perl" highlighed.') %></li>
	    <li><%$ __('Drag the file "perl" to the System Preferences and drop it there, so it looks like this:') %>
		<br/>
		<img src=/res/macos-security-settings-screenshot.png></img>
		<br/><%= __('(i.e. "perl" should appear in the list).') %>
	    </ol>
	    <p/><p/>
	    <%= __("Once you do the above, retry last operation after which you've got this message") %>
	</div>
    </div>

</div>

