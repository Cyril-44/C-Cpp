This directory is appended to the Include Path List - so if a module with
same name is installed in the $perlroot/site_perl directory, it will be
used from there rather than from the deps-fallbacks directory.

File/Spec* is unmodified copy of File::Spec version 0.82.
