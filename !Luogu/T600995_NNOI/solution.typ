= 序列 (ball) 题解

== 题目大意
给定 $n$ 个数（$n$ 为奇数），可以进行 $m$ 次操作，每次任选一个数加上 $[-x, x]$ 之间的整数。求操作结束后，序列的中位数有多少种不同的取值？

== 解题思路

中位数是排序后的第 $k = (n+1)/2$ 个数。要让中位数尽可能大或尽可能小，我们需要把有限的操作次数 $m$ “花在刀刃上”。

假设排序后的序列为 $a_1, a_2, ..., a_n$。

=== 10% 数据：暴力搜索
由于 $n, m, x$ 都很小，可以通过搜索或动态规划枚举每一次操作加了多少。但这种方法效率极低，仅供理解题目。

=== 40% 数据：二分或简单贪心
对于中等规模数据，我们可以思考：如果想让中位数达到某个值 $V$，最少需要多少次操作？
- 要让中位数 *变大*：我们需要把序列中最小的 $k$ 个数都提升到 $V$。
- 要让中位数 *变小*：我们需要把序列中最大的 $k$ 个数都降低到 $V$。

=== 100% 数据：贪心 + 范围计算
观察发现，中位数的取值一定是连续的一段整数。我们只需要求出中位数的**最小值** $L$ 和**最大值** $R$，答案就是 $R - L + 1$。

1. *求最大值 $R$：*
   为了让第 $k$ 小的数尽可能大，我们应该选出序列中最小的 $k$ 个数。
   每次操作可以给一个数增加 $x$。总共可以增加 $m dot x$。
   我们要平摊这 $m dot x$ 的增量，使得这 $k$ 个数中最小的那个（即第 $k$ 个数）尽可能大。
   - 将 $a_1, a_2, dots, a_k$ 从小到大排序。
   - 目标是让 $a_k$ 最大。

2. *求最小值 $L$：*
   同理，为了让第 $k$ 小的数尽可能小，我们选出序列中最大的 $k$ 个数（即从 $a_k$ 到 $a_n$）。
   每次操作可以给一个数减去 $x$。总共可以减去 $m dot x$。

*结论：*
设排序后的序列为 $a$。
- 最大中位数 $R$：考虑前 $k$ 个数。总和 $S_{max} = sum_{i=1}^k a_i + m dot x$。$R = floor(S_{max} / k)$。
  注意：$R$ 不能小于初始的 $a_k$，且受限于平摊后的最大值。更简单的方法是：把能量分给所有小于等于当前中位数的数。
  *修正贪心策略：* 实际上，最简单的思路是直接对前 $k$ 个数求平均值。

== 核心代码 (C++)

```cpp
#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

typedef long long ll;

int main() {
    ll n, m, x;
    cin >> n >> m >> x;
    vector<ll> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];
    sort(a.begin(), a.end());

    ll k = (n + 1) / 2;
    ll total_change = m * x;

    // 计算最大中位数 R
    // 考虑前 k 个数，把增量分给它们
    ll sum_low = 0;
    for (int i = 0; i < k; i++) sum_low += a[i];
    ll R = (sum_low + total_change) / k;
    // R 至少是 a[k-1] 没变之前的情况，但公式已经涵盖了

    // 计算最小中位数 L
    // 考虑后 k 个数 (索引从 k-1 到 n-1)，把减量分给它们
    ll sum_high = 0;
    for (int i = k - 1; i < n; i++) sum_high += a[i];
    // 为了方便计算平均值，先算出总和减去 total_change
    ll L_sum = sum_high - total_change;
    ll L;
    if (L_sum >= 0) L = L_sum / k;
    else L = (L_sum - k + 1) / k; // 负数下取整技巧

    cout << R - L + 1 << endl;

    return 0;
}
```