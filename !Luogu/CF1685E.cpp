#include <cstdio>
constexpr int N = 200004;
int p[N];
int main() {
    int n, q;
    scanf("%d%d", &n, &q);
    for (int i = 1; i <= 2*n+1; i++) scanf("%d", &p[i]);
    while (q--) {
        
    }
    return 0;
}