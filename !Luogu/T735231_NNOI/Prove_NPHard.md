
没有 $O(n \log n)$ 或者同等级别的做法。

证明：假设我们将点按 $w_i$ 降序排序。在最坏情况下，每次添加新点 $j$ 时，$v_j$ 是当前前缀中最大的，此时 $cnt=0$，第一个部分仅更新 $ans[1]$，该过程的总操作次数为 $\sum_{j=1}^n O(j) = \Omega(n^2)$。每个操作至少涉及计算候选值并可能更新 $ans[k]$，因此时间下界为 $\Omega(n^2)$。