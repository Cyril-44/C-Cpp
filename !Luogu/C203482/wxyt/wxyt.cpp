#include <bits/stdc++.h>
const int N = 200005;
int a[N];
int main() {
    int n, q, w, l, r, d;
    scanf("%d%d%d", &n, &q, &w);
    for (int i = 1; i <= n; i++)
        scanf("%d", &a[i]);
    while (q--) {
        scanf("%d%d%d", &l, &r, &d);
        
    }
    return 0;
}