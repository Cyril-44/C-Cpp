--- ./2.2.4.7-7B.md	2026-08-21 17:34:21.197248981 +0800
+++ "./2.2.4.7-7B fix.md"	2026-08-21 17:34:07.721220894 +0800
@@ -35,10 +35,10 @@
     int start = 1;
     // 欧拉回路要求所有顶点度数为偶数
     for (int u = 1; u <= n; u++){
-        if (deg[u] % 2 != 0) return vector<int>();
+        if (deg[u] % 2 != 0) return {};
         if (deg[u] > 0) start = u;
     }
-    if (m == 0) return vector<int>(1, 1);
+    if (m == 0) return {1};
 
     fill(used + 1, used + m + 1, false);
     pos.assign(n + 1, 0);
@@ -46,7 +46,7 @@
     dfs(start);
 
     // 少于 m + 1 个点说明非零度部分不连通
-    if (int(ans.size()) != m + 1) return vector<int>();
+    if (int(ans.size()) != m + 1) return {};
     reverse(ans.begin(), ans.end());
     return ans;
 }
