构造一个长度为 $n$ 的序列 $a$（顺序不重要，可视为可重集）且满足以下性质：

1. $\sum\limits_{i=1}^{n} a_{i} = 2n - 2$。

2. 所有由 $n$ 个节点构成且满足每个节点的度数构成的可重集等于 $a$ 的树（边是无向的）中，直径最大为 $x$（$x = -1$ 表示无限制），最小为 $y$（$y = -1$ 表示无限制）。

如果这样的序列不存在，输出 $-1$。