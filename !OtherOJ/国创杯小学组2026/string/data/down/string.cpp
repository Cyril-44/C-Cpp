#include <cstdio>
#include <cstring>
#include <algorithm>
constexpr int MAXN = 5000000;
char s[MAXN + 5];
int main() {
    int C, t, n;
    scanf("%d%d", &C, &t);
    while (t--) {
        scanf("%s", s+1);
        n = strlen(s+1);

        // Write your code here.
    }
    return 0;
}