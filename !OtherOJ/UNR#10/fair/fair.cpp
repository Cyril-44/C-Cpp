#include <bits/stdc++.h>
constexpr int N = 15;
int a[N];
int main() {
    int n, m, k;
    scanf("%d%d%d", &n, &m, &k);
    for (int i = 1; i <= n; i++)
        scanf("%d", &a[i]);
    
    return 0;
}