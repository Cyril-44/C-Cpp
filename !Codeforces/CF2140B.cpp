#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t, x;
    scanf("%d", &t);
    while (t--) {
        scanf("%d", &x);
        printf("%d\n", x * 8);
    }
    return 0;
}