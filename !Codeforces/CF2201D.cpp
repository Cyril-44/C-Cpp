#include <stdio.h>
#include <set>
constexpr int N = 200005;
std::set<int> pos[N];
inline void insert()
int main() {
    int T, n, q;
    scanf("%d", &T);
    while (T--) {
        scanf("%d%d", &n, &q);
        
    }
    return 0;
}